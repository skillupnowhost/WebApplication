/**
 * The hero artwork shows the AI agent's held globe sitting left-of-centre.
 * These percentages are hand-tuned against that artwork so the orbiting
 * icon shells in HeroOrbitIcons stay centred on the globe.
 */
export const HERO_ORBIT_CENTER = { left: 26, top: 37 };
