export type GalleryItemData = {
  id: string;
  imageUrl: string;
  caption: string;
  category: string;
};

export const CATEGORY_ORDER = ["Event", "Workshop", "Office", "Team Activity", "Certificate & Achievement"];
