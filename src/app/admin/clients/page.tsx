import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Clients — MyLoginn Admin" };

export default function AdminClientsPage() {
  return <EntityScreen entity="clients" />;
}
