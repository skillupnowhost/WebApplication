import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Events — MyLoginn Admin" };

export default function AdminEventsPage() {
  return <EntityScreen entity="events" />;
}
