import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Partners — MyLoginn Admin" };

export default function AdminPartnersPage() {
  return <EntityScreen entity="partners" />;
}
