import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Gallery — MyLoginn Admin" };

export default function AdminGalleryPage() {
  return <EntityScreen entity="gallery" />;
}
