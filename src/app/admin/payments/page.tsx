import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Payments — MyLoginn Admin" };

export default function AdminPaymentsPage() {
  return <EntityScreen entity="payments" />;
}
