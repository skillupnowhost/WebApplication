import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Company Milestones — MyLoginn Admin" };

export default function AdminMilestonesPage() {
  return <EntityScreen entity="milestones" />;
}
