import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Tutoring — MyLoginn Admin" };

export default function AdminTutoringPage() {
  return <EntityScreen entity="tutoring" />;
}
