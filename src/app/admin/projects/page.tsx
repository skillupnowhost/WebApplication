import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Projects — MyLoginn Admin" };

export default function AdminProjectsPage() {
  return <EntityScreen entity="projects" />;
}
