import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Internships — MyLoginn Admin" };

export default function AdminInternshipsPage() {
  return <EntityScreen entity="internships" />;
}
