import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Key Offerings — MyLoginn Admin" };

export default function AdminOfferingsPage() {
  return <EntityScreen entity="offerings" />;
}
