import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Categories — MyLoginn Admin" };

export default function AdminCategoriesPage() {
  return <EntityScreen entity="categories" />;
}
