import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Team — MyLoginn Admin" };

export default function AdminTeamPage() {
  return <EntityScreen entity="team" />;
}
