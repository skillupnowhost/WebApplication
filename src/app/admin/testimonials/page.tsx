import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Testimonials — MyLoginn Admin" };

export default function AdminTestimonialsPage() {
  return <EntityScreen entity="testimonials" />;
}
