import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Mentors — MyLoginn Admin" };

export default function AdminMentorsPage() {
  return <EntityScreen entity="mentors" />;
}
