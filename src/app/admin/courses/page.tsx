import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Courses — MyLoginn Admin" };

export default function AdminCoursesPage() {
  return <EntityScreen entity="courses" />;
}
