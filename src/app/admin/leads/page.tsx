import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Leads — MyLoginn Admin" };

export default function AdminLeadsPage() {
  return <EntityScreen entity="leads" />;
}
