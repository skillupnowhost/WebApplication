import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Enrollments — MyLoginn Admin" };

export default function AdminEnrollmentsPage() {
  return <EntityScreen entity="enrollments" />;
}
