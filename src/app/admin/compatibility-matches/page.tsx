import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "Compatibility Matches — MyLoginn Admin" };

export default function AdminCompatibilityMatchesPage() {
  return <EntityScreen entity="astrologyMatches" />;
}
