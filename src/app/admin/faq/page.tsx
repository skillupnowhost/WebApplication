import { EntityScreen } from "@/components/admin/EntityScreen";

export const metadata = { title: "FAQs — MyLoginn Admin" };

export default function AdminFaqPage() {
  return <EntityScreen entity="faq" />;
}
